import { UpdateProductsCommand } from './update-products.command';

describe('UpdateProductsCommand', () => {
  it('should create an instance', () => {
    expect(new UpdateProductsCommand()).toBeTruthy();
  });
});
