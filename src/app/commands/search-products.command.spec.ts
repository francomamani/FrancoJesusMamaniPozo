import { SearchProductsCommand } from './search-products.command';

describe('SearchProductsCommand', () => {
  it('should create an instance', () => {
    expect(new SearchProductsCommand()).toBeTruthy();
  });
});
