import { RetrieveProducts } from './retrieve-products.command';

describe('RetrieveProducts', () => {
  it('should create an instance', () => {
    expect(new RetrieveProducts()).toBeTruthy();
  });
});
