import { RemoveProductCommand } from './remove-product.command';

describe('RemoveProductCommand', () => {
  it('should create an instance', () => {
    expect(new RemoveProductCommand()).toBeTruthy();
  });
});
