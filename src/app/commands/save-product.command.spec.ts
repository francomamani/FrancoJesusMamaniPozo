import { SaveProductCommand } from './save-product.command';

describe('SaveProductCommand', () => {
  it('should create an instance', () => {
    expect(new SaveProductCommand()).toBeTruthy();
  });
});
